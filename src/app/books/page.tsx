'use client';
import { useQuery } from '@tanstack/react-query';
import Link from 'next/link';
import { api } from '@/lib/api';
type Book = { id: number; title: string; description?: string; price: string | number; stock: number };
async function getBooks(): Promise<Book[]> { const { data } = await api.get('/books'); return data; }
export default function BooksPage() { const { data, isLoading, error } = useQuery({ queryKey: ['books'], queryFn: getBooks }); return <main className="min-h-screen bg-slate-50"><header className="border-b bg-white"><div className="mx-auto flex w-full max-w-7xl items-center justify-between px-6 py-4"><Link href="/" className="text-xl font-bold text-orange-600">Book Shop</Link><Link href="/" className="text-sm">Trang chủ</Link></div></header><section className="mx-auto w-full max-w-7xl px-6 py-12"><h1 className="mb-8 text-3xl font-bold">Danh sách sách</h1>{isLoading && <p>Đang tải sách...</p>}{error && <p className="text-red-600">Không thể tải dữ liệu sách.</p>}<div className="grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-4">{data?.map((book) => <article key={book.id} className="rounded-xl border bg-white p-5 shadow-sm"><div className="mb-5 flex aspect-[4/5] items-center justify-center rounded-lg bg-orange-50 text-5xl">📚</div><h2 className="font-semibold">{book.title}</h2><p className="mt-2 line-clamp-2 text-sm text-slate-500">{book.description || 'Khám phá cuốn sách này.'}</p><div className="mt-4 flex items-center justify-between"><span className="font-bold text-orange-600">{Number(book.price).toLocaleString('vi-VN')}đ</span><span className="text-xs text-slate-500">Còn {book.stock}</span></div></article>)}</div></section></main> }
