'use client';
import { useCartStore } from '@/stores/cart.store';
import Link from 'next/link';
export default function CartPage() { const items = useCartStore((s) => s.items); const remove = useCartStore((s) => s.remove); const total = useCartStore((s) => s.total); return <main className="min-h-screen bg-slate-50 px-6 py-12"><div className="mx-auto max-w-3xl"><Link href="/books" className="text-orange-600">← Tiếp tục mua sắm</Link><h1 className="mt-6 text-3xl font-bold">Giỏ hàng</h1>{items.length === 0 ? <p className="mt-6 text-slate-500">Giỏ hàng đang trống.</p> : <div className="mt-6 space-y-3">{items.map((item) => <div key={item.bookId} className="flex items-center justify-between rounded-lg border bg-white p-4"><div><p className="font-medium">{item.title}</p><p className="text-sm text-slate-500">{item.quantity} × {item.price.toLocaleString('vi-VN')}đ</p></div><button onClick={() => remove(item.bookId)} className="text-sm text-red-600">Xóa</button></div>)}<div className="flex justify-between border-t pt-5 text-xl font-bold"><span>Tổng</span><span className="text-orange-600">{total().toLocaleString('vi-VN')}đ</span></div></div>}</div></main> }
